extends layout

block content
  form(role="form", action="/", method="post", enctype="multipart/form-data")
    label(for="onixfile") Upload File
    input(type="file", name="onixfile", id="onixfile")
    label(for="submit")
    input(type="submit", name="submit", value="submit")
